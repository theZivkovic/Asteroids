const config = {
    player: {
        rotationSpeed: 0.1,
        bodyWidth: 20,
        bodyHeight: 40,
        bodyColor: 0xFFFFFF,
        cooldownColor: 0xFFFF00,
        cooldownTimeMs: 3000
    }
}

export default config;