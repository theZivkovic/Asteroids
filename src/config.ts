const config = {
    player: {
        rotationSpeed: 0.1,
        bodyWidth: 20,
        bodyHeight: 40,
        bodyColor: 0xFFFFFF,
        cooldownColor: 0xFF0000,
        fireColor: 0xFFFF00,
        cooldownTimeMs: 2000,
        acceleration: 0.15,
        maxSpeed: 6
    }
}

export default config;